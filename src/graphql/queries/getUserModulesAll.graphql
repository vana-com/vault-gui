query getUserModulesAll($userId: uuid!) {
  usersModules(
    where: { userId: { _eq: $userId }, progress: { _eq: "COMPLETE" } }
  ) {
    id
    module {
      name
    }
    moduleId
    urlToData
  }
}
