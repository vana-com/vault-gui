const runDataQueryPipeline = (sqlQuery: string): Array<any> => {
  // download data
  // unencrypt file
  // structure data
  // run SQL query
  const sqlQueryData = [sqlQuery];
  return sqlQueryData;
};

export { runDataQueryPipeline };
