import tw from 'twin.macro'

/* Set the motion.div to always fill the space provided by the parent. This ensures smooth transitions */

export const MotionDiv = tw.div`absolute inset-0 z-10 h-full`

export const styledMotionDiv = tw`absolute inset-0 z-10 h-full`
